---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Receiver
metadata:
  name: home-cluster
  namespace: flux-system
spec:
  type: github
  events:
  - "ping"
  - "push"
  secretRef:
    name: github-webhook-token
  resources:
  - kind: GitRepository
    name: flux-system
  - kind: Kustomizations
    name: apps
  - kind: Kustomizations
    name: core
  - kind: Kustomizations
    name: crds
