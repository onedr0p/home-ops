loki:
  config:
    storage_config:
      aws:
        endpoint: "${LOKI_B2_ENDPOINT}/${LOKI_B2_BUCKET_NAME}"
        access_key_id: "${LOKI_B2_KEY_ID}"
        secret_access_key: "${LOKI_B2_APP_KEY}"
        insecure: false
        sse_encryption: false
        http_config:
          idle_conn_timeout: 90s
          response_header_timeout: 0s
          insecure_skip_verify: false
        s3forcepathstyle: true
