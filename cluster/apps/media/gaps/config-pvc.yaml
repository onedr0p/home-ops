---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gaps-config-v1
  namespace: media
spec:
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
