---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: gaps
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      chart: /charts/kah-common/
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 5m
  values:
    fullnameOverride: gaps
    image:
      repository: docker.io/housewrecker/gaps
      tag: latest@sha256:5179911164bbe8e5b30307c71409eba03712bba1359c5d924ba66cc3fe7d435d
    env:
      TZ: "America/New_York"
    service:
      main:
        ports:
          http:
            port: 8484
    ingress:
      main:
        enabled: true
        ingressClassName: "nginx"
        hosts:
          - host: "gaps.${SECRET_PUBLIC_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - "gaps.${SECRET_PUBLIC_DOMAIN}"
    persistence:
      config:
        enabled: true
        existingClaim: gaps-config-v1
        mountPath: /usr/data
    resources:
      requests:
        memory: 100Mi
        cpu: 100m
      limits:
        memory: 750Mi
