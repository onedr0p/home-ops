---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: &app gaps
  namespace: media
  labels:
    app.kubernetes.io/name: *app
    app.kubernetes.io/instance: *app
spec:
  ingressClassName: nginx
  rules:
    - host: &host "gaps.${SECRET_PUBLIC_DOMAIN}"
      http:
        paths:
          - backend:
              service:
                name: *app
                port:
                  number: 8484
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - *host
