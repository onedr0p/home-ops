---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: plex-off-deck
  namespace: media
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: "Forbid"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      backoffLimit: 3
      ttlSecondsAfterFinished: 300
      template:
        spec:
          automountServiceAccountToken: false
          restartPolicy: OnFailure
          containers:
            - name: jbops
              image: ghcr.io/onedr0p/jbops:1.0.880@sha256:a63977bfee226d1406e8f474ca65947dd29e0d3e19cddd55882ecfd65b56985b
              env:
                - name: TZ
                  value: "${TIMEZONE}"
                - name: JBOPS__SCRIPT_PATH
                  value: utility/off_deck.py
              args:
                - /entrypoint.sh
                - --playlists
                - "Smart Toons"
              volumeMounts:
                - name: config
                  mountPath: /config/config.ini
                  subPath: config.ini
                  readOnly: true
          volumes:
            - name: config
              secret:
                secretName: plex-jbops
                items:
                  - key: config.ini
                    path: config.ini
