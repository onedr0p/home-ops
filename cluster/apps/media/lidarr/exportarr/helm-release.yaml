---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app lidarr-exporter
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      chart: kah-common-chart
      version: 1.1.2
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: flux-system
      interval: 5m
  install:
    createNamespace: true
  dependsOn:
    - name: lidarr
      namespace: media
  values:
    global:
      nameOverride: *app
    image:
      repository: ghcr.io/onedr0p/exportarr
      tag: v1.0.0
    env:
      TZ: "${TIMEZONE}"
      PORT: "7373"
      URL: "lidarr.media.svc.cluster.local:8686"
      ENABLE_ADDITIONAL_METRICS: "false"
      ENABLE_UNKNOWN_QUEUE_ITEMS: "false"
    envFrom:
      - secretRef:
          name: *app
    service:
      main:
        enabled: false
    resources:
      requests:
        cpu: 100m
        memory: 100Mi
      limits:
        memory: 250Mi
