---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: internal-dns
  namespace: networking
spec:
  endpoints:
  - dnsName: ext.devbu.io
    recordType: A
    targets:
    - "${CLUSTER_LB_TRAEFIK}"
