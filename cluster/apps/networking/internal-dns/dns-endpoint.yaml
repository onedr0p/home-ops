---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: ${SECRET_CLUSTER_DOMAIN/./-}-gateway
  namespace: networking
spec:
  endpoints:
  - dnsName: "gateway.${SECRET_CLUSTER_DOMAIN}"
    recordType: CNAME
    targets:
    - "${CLUSTER_LB_TRAEFIK}"
