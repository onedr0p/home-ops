---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: ${SECRET_CLUSTER_DOMAIN/./-}-ext
  namespace: networking
spec:
  endpoints:
  - dnsName: "ext.${SECRET_CLUSTER_DOMAIN}"
    recordType: A
    targets:
    - "${CLUSTER_LB_TRAEFIK}"
