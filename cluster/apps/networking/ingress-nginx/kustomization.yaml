---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: networking
resources:
  - dashboard
  - helm-release.yaml
configMapGenerator:
  - name: allow-networks
    files:
      - allow-networks.txt
generatorOptions:
  disableNameSuffixHash: true
