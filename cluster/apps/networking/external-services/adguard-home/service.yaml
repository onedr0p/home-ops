---
apiVersion: v1
kind: Service
metadata:
  name: adguard-home
  namespace: networking
spec:
  type: ExternalName
  externalName: opnsense.${SECRET_PUBLIC_DOMAIN}.
  ports:
    - name: http
      port: &port 3010
      protocol: TCP
      targetPort: *port
