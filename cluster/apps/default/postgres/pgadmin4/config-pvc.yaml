---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pgadmin4-config-v1
  namespace: default
  labels:
    app.kubernetes.io/name: &name pgadmin4
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
