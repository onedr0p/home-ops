---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cron-job.yaml
namespace: default
secretGenerator:
  - name: plex-jobs
    files:
      - config.ini=config/config.sops.toml
generatorOptions:
  disableNameSuffixHash: true
