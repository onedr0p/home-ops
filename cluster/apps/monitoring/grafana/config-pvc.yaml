---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-config-v1
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
