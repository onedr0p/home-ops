---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flame-config-v1
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
