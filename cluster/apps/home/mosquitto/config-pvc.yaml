---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mosquitto-config-v1
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: ceph-block
