---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-config-v2
  namespace: home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-block
