---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - config-pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
namespace: home
configMapGenerator:
  - name: frigate-config
    files:
      - frigate.yaml
generatorOptions:
  disableNameSuffixHash: true
