---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: home
resources:
  - config-pvc.yaml
  - helm-release.yaml
configMapGenerator:
  - name: frigate-config.yaml
    files:
      - frigate-config.yaml
generatorOptions:
  disableNameSuffixHash: true
