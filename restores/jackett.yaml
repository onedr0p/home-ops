---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: jackett-restore
  namespace: default
spec:
  repository:
    name: deploy-jackett
  rules:
  - snapshots:
    - latest
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: jackett
    volumeMounts:
    - name:  config
      mountPath:  /config
