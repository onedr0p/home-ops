---
apiVersion: stash.appscode.com/v1beta1
kind: RestoreSession
metadata:
  name: ombi-restore
  namespace: default
spec:
  repository:
    name: deploy-ombi
  rules:
  - snapshots:
    - latest
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: ombi
    volumeMounts:
    - name:  config
      mountPath:  /config
