---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: "Upgrade Cluster"

on:
  workflow_dispatch:
    inputs:
      prNumber:
        description: PR Number
        required: true

jobs:
  upgrade:
    name: Upgrade Cluster
    runs-on: ["arc-runner-set-home-ops"]
    steps:
      - name: Generate Token
        uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: "${{ secrets.BOT_APP_ID }}"
          private-key: "${{ secrets.BOT_APP_PRIVATE_KEY }}"

      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: "${{ steps.app-token.outputs.token }}"

      - name: Setup Homebrew
        uses: Homebrew/actions/setup-homebrew@master

      - name: Setup Workflow Tools
        shell: bash
        run: brew install kubectl krew

      - name: Install krew plugins
        shell: bash
        run: kubectl krew install cnpg

      - name: Write kubeconfig
        id: kubeconfig
        uses: timheuer/base64-to-file@v1
        with:
          encodedString: "${{ secrets.KUBECONFIG }}"
          fileName: kubeconfig

      - name: Print cnpg status
        shell: bash
        env:
          KUBECONFIG: "${{ steps.kubeconfig.outputs.filePath }}"
        run: kubectl cnpg --context main -n database status postgres16

      # - name: Set maintenance mode
      #   shell: bash
      #   env:
      #     KUBECONFIG: "${{ steps.kubeconfig.outputs.filePath }}"
      #   run: kubectl cnpg --context main maintenance unset --reusePVC --all-namespaces

      # - name: Merge cluster upgrade PR
      #   shell: bash
      #   env:
      #     GITHUB_TOKEN: "${{ steps.app-token.outputs.token }}"
      #   run: gh pr merge ${{ github.event.inputs.prNumber }} --squash
