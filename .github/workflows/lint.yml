name: lint
on:
  push:
    branches:
      - master
jobs:
#  helmrelease:
#    runs-on:
#      - ubuntu-latest
#    if: "!contains(github.event.head_commit.message, '[ci-skip]')"
#    steps:
#      - uses: actions/checkout@v2
#      - name: Lint
#       run: |
#          sudo curl -L -o hrval-all.sh https://raw.githubusercontent.com/stefanprodan/hrval-action/master/src/hrval-all.sh
#          sudo chmod +x hrval-all.sh
#          sudo curl -L -o hrval.sh https://raw.githubusercontent.com/stefanprodan/hrval-action/master/src/hrval.sh
#          sudo sed -i -e 's/helmv3/helm/g' hrval.sh
#          sudo chmod +x hrval.sh
#          ./hrval-all.sh deployments false 1.18.8 v3
  lint-yaml:
    runs-on:
      - ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci-skip]')"
    steps:
      - uses: actions/checkout@v2
      - name: Lint yaml
        uses: ibiqlik/action-yamllint@latest
  lint-shell:
    runs-on:
      - ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci-skip]')"
    steps:
      - uses: actions/checkout@v2
      - name: Lint scripts
        uses: azohra/shell-linter@latest
        with:
          path: "hack"
          severity: "error"
