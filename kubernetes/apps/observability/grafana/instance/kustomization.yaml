---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./grafanadatasource.yaml
  - ./grafana.yaml
  - ./httproute.yaml
  - ./sidecar-rbac.yaml
configMapGenerator:
  - name: sidecar-provider-configmap
    files:
      - sidecar-provider.yaml=./resources/sidecar-provider.yaml
configurations:
  - ./sidecar-transformer.yaml
