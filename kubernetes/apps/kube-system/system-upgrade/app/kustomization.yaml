---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./rbac.yaml
configMapGenerator:
  - name: system-upgrade-versions
    env: ./resources/versions.env
generatorOptions:
  disableNameSuffixHash: true
