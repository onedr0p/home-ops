---
apiVersion: v1
kind: ConfigMap
metadata:
  name: rook-gatus-ep
  namespace: rook-ceph
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: rook
        group: guarded
        url: 1.1.1.1
        interval: 1m
        ui:
          hide-hostname: true
          hide-url: true
        dns:
          query-name: rook.devbu.io
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
