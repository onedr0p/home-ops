---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  # name: populator-test-config
  name: config-populator-test-0 # must be named the same as the generated VCT PVC
  namespace: default
  # labels:
  #   kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes: ["ReadWriteOnce"]
  dataSourceRef:
    kind: ReplicationDestination
    apiGroup: volsync.backube
    name: populator-test-dst
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
