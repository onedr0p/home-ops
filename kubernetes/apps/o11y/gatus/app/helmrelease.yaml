---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gatus
spec:
  chartRef:
    kind: OCIRepository
    name: gatus
  interval: 1h
  values:
    fullnameOverride: gatus
    # Reloader reads the workload, not the pod: roll gatus when the mounted
    # ConfigMap or the envFrom Secret changes.
    deploymentAnnotations:
      reloader.stakater.com/auto: "true"
    gatus:
      delayStartSeconds: "5"
      env:
        TZ: America/New_York
      envFrom:
        - secretRef:
            name: gatus-secret
      resources:
        requests:
          cpu: 100m
        limits:
          memory: 256Mi
    sidecar:
      kinds:
        httproute:
          enable: true
          auto: true
        service:
          enable: true
      resources:
        requests:
          cpu: 10m
        limits:
          memory: 128Mi
    config:
      existingConfigMap: gatus-configmap
      items:
        - key: config.yaml
          path: config.yaml
        - key: buddy.yaml
          path: buddy.yaml
    persistence:
      # Reuse the PVC left behind by the previous StatefulSet's
      # volumeClaimTemplate so the sqlite uptime history carries over.
      enabled: true
      existingClaim: config-gatus-0
    monitoring:
      serviceMonitor:
        enabled: true
    httpRoute:
      enabled: true
      annotations:
        gatus.home-operations.com/enabled: "false"
      hostnames:
        - gatus.turbo.ac
        - status.turbo.ac
      parentRefs:
        - name: envoy-external
          namespace: network
