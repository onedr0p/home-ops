---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - ./configmap.yaml
  - ./helmrelease.yaml
configMapGenerator:
  - name: dell-idrac-dashboard
    files:
      - dell-idrac-dashboard.json=./dashboard.json
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
  labels:
    grafana_dashboard: "true"
labels:
  - pairs:
      app.kubernetes.io/name: snmp-exporter-dell-idrac
      app.kubernetes.io/instance: snmp-exporter-dell-idrac
      app.kubernetes.io/part-of: snmp-exporter
