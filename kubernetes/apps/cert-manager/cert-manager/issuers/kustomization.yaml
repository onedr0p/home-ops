---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
configMapGenerator:
  - name: cert-manager-cluster-issuers
    files:
      - clusterissuer.yaml=./values/clusterissuer.yaml
      - externalsecret.yaml=./values/externalsecret.yaml
configurations:
  - kustomizeconfig.yaml
