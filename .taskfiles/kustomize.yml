---
version: '3'

tasks:

  generate:
    desc: Generate kustomizations
    cmds:
    - "{{.PROJECT_DIR}}/hack/gen-kustomize.sh"
    preconditions:
    - "test -f {{.PROJECT_DIR}}/hack/gen-kustomize.sh"
    silent: true
