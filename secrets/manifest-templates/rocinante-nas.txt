apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: rocinante-nas
  annotations:
    kubernetes.io/ingress.class: internal
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  rules:
  - host: "rocinante.${DOMAIN}"
    http:
      paths:
      - backend:
          serviceName: rocinante-nas
          servicePort: 8080
  tls:
  - hosts:
    - "rocinante.${DOMAIN}"
---
apiVersion: v1
kind: Service
metadata:
  name: rocinante-nas
spec:
  ports:
  - name: http
    port: 8080
  type: ExternalName
  externalName: 192.168.1.170