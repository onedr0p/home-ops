ingress:
  hosts: [piaware.$DOMAIN]
  tls:
  - hosts:
    - piaware.$DOMAIN
  annotations:
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: nginx-basic-auth
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required"
latitude: $LATITUDE
longitude: $LONGITUDE