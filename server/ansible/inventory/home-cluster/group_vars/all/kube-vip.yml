---

# Use kube-vip to publish a HA IP address for the control-plane
kube_vip:
  enabled: true

  # Interface kube-vip should bind to
  listen_interface: eno1

  # Determines if the RBAC manifests get deployed
  rbac:
    enabled: true
