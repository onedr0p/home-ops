---

- name: cluster | metrics | deploy prometheus monitoring
  become: true
  # run_once: true
  ansible.builtin.template:
    src: "metrics.yaml.j2"
    dest: "{{ k3s_server_manifests_dir }}/metrics.yaml"
    mode: 0644
