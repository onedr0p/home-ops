---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: helm-operator
  namespace: flux
spec:
  releaseName: helm-operator
  chart:
    repository: https://charts.fluxcd.io/
    name: helm-operator
    version: 0.5.0
  valuesFrom:
  - externalSourceRef:
      url: https://raw.githubusercontent.com/onedr0p/k3s-gitops/master/deployments/flux/helm-operator/helm-operator-values.yaml
