createCRD: true
git:
  ssh:
    secretName: flux-git-deploy
image:
  repository: docker.io/fluxcd/helm-operator
  tag: 1.0.0-rc7
  pullPolicy: IfNotPresent
helm:
  versions: "v2"
prometheus:
  enabled: true