image:
  repository: docker.io/fluxcd/helm-operator
  tag: 1.0.0-rc8
createCRD: true
git:
  ssh:
    secretName: flux-git-deploy
helm:
  versions: "v3"
prometheus:
  enabled: true
  serviceMonitor:
    create: true
    namespace: flux
    interval: 5s
    scrapeTimeout: 10s
