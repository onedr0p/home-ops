createCRD: true
git:
  ssh:
    secretName: flux-git-deploy
helm:
  versions: "v3"
prometheus:
  enabled: false
  serviceMonitor:
    create: true
    namespace: flux
    interval: 30s
    scrapeTimeout: 10s