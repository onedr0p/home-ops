powerdns:
  domain: "${DOMAIN}"
  config:
    default-soa-mail: "${EMAIL}"
    default-soa-name: "ns1.${DOMAIN}"
    default-ttl: 300
    master: yes
  api_key: "${POWERDNS_API_KEY}"
  mysql:
    password: "${POWERDNS_DB_USER_PASSWORD}"
mariadb:
  rootUser:
    password: "${POWERDNS_DB_ROOT_PASSWORD}"
  db:
    password: "${POWERDNS_DB_USER_PASSWORD}"