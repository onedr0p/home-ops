powerdns:
  domain: $DOMAIN
  default_soa_name: ns1.$DOMAIN
  mysql_rootpass: $POWERDNS_DB_ROOT_PASSWORD
  mysql_pass: $POWERDNS_DB_USER_PASSWORD
mariadb:
  mysql_rootpass: $POWERDNS_DB_ROOT_PASSWORD
  mysql_pass: $POWERDNS_DB_USER_PASSWORD
powerdnsadmin:
  ingress:
    hosts: 
    - "pdns-admin.${DOMAIN}"
    tls:
    - hosts:
      - "pdns-admin.${DOMAIN}"