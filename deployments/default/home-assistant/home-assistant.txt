ingress:
  hosts:
  - "hass.${DOMAIN}"
  tls:
  - hosts:
    - "hass.${DOMAIN}"
vscode:
  ingress:
    hosts:
    - "hass-editor.${DOMAIN}"
    tls:
    - hosts:
      - "hass-editor.${DOMAIN}"
  extraEnv:
    HASS_SERVER: http://localhost:8123
    HASS_TOKEN: $HASS_TOKEN
