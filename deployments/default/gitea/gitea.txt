ingress:
  hosts: 
  - "git.${DOMAIN}"
  tls:
  - hosts:
    - "git.${DOMAIN}"
gitea:
  config:
    adminPassword: "${GITEA_ADMIN_PASSWORD}"
    adminEmail: "${GMAIL_USER}"
  server:
    http: 
      externalDomain: "git.${DOMAIN}"
    ssh:
      externalDomain: "git.${DOMAIN}"
  security:
    secretKey: "${GITEA_SECRET_KEY}"