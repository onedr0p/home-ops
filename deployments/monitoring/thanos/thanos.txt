query:
  http:
    ingress:
      hosts: 
        - "thanos.${DOMAIN}"
      tls:
      - hosts: 
        - "thanos.${DOMAIN}"
objstore:
  config:
    access_key: "${MINIO_ACCESS_KEY}"
    secret_key: "${MINIO_SECRET_KEY}"