---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: lidarr-test
  namespace: testing
  annotations:
    fluxcd.io/ignore: "false"
    fluxcd.io/automated: "true"
    fluxcd.io/tag.chart-image: semver:~0.8.0.
spec:
  releaseName: lidarr-test
  rollback:
    enable: false
  chart:
    repository: https://k8s-at-home.com/charts/
    name: lidarr
    version: 5.0.1
  values:
    controllerType: deployment
    image:
      repository: k8sathome/lidarr
      tag: 0.8.0.1886
      pullPolicy: Always
    env:
      TZ: "America/New_York"
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "internal"
      hosts:
      - host: lidarr-test.devbu.io
        paths:
        - path: /
      tls:
      - hosts:
        - "lidarr-test.devbu.io"
    persistence:
      config:
        enabled: true
        storageClass: "longhorn"
        accessMode: ReadWriteOnce
        size: "1Gi"
    resources:
      requests:
        memory: 250Mi
        cpu: 500m
      limits:
        memory: 750Mi
        cpu: 1000m
