---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: qbittorrent-test
  namespace: testing
  annotations:
    fluxcd.io/ignore: "false"
    fluxcd.io/automated: "true"
    fluxcd.io/tag.chart-image: semver:~4.3.
spec:
  releaseName: qbittorrent-test
  rollback:
    enable: false
  chart:
    repository: https://k8s-at-home.com/charts/
    name: qbittorrent
    version: 6.0.1
  values:
    controllerType: deployment
    image:
      repository: k8sathome/qbittorrent
      tag: 4.3.0
      pullPolicy: Always
    env:
      TZ: "America/New_York"
    podSecurityContext:
      runAsUser: 1000
      runAsGroup: 1000
      fsGroup: 1000
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "internal"
      hosts:
      - host: qbittorrent-test.devbu.io
        paths:
        - path: /
      tls:
      - hosts:
        - "qbittorrent-test.devbu.io"
    persistence:
      config:
        enabled: true
        storageClass: "longhorn"
        accessMode: ReadWriteOnce
        size: "1Gi"
    resources:
      requests:
        memory: 250Mi
        cpu: 500m
      limits:
        memory: 750Mi
        cpu: 1000m
