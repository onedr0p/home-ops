---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: nzbhydra2-test
  namespace: testing
  annotations:
    fluxcd.io/ignore: "false"
    fluxcd.io/automated: "true"
    fluxcd.io/tag.chart-image: semver:~3.
spec:
  releaseName: nzbhydra2-test
  rollback:
    enable: false
  chart:
    repository: https://k8s-at-home.com/charts/
    name: nzbhydra2
    version: 4.0.0
  values:
    controllerType: deployment
    image:
      repository: k8sathome/nzbhydra2
      pullPolicy: Always
      tag: 3.4.3
    env:
      TZ: "America/New_York"
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: "internal"
      hosts:
      - host: nzbhydra2-test.devbu.io
        paths:
        - path: /
      tls:
      - hosts:
        - "nzbhydra2-test.devbu.io"
    persistence:
      config:
        enabled: true
        storageClass: "longhorn"
        accessMode: ReadWriteOnce
        size: "1Gi"
    resources:
      requests:
        memory: 250Mi
        cpu: 500m
      limits:
        memory: 1000Mi
        cpu: 1000m
