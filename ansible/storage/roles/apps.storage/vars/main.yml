---
#
# SECRET* vars are encrypted with sops
#

# -- Base Docker Directory
docker_base_dir: /opt/docker

# -- Base ZFS Dataset
zfs_zpool_base_dir: /tycho

# -- Minio Data Directory
minio_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/Minio"

# -- Nexus Data Directory
nexus_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/Nexus"

# -- Postgresql Data Directory
postgresql_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/Postgresql"

# -- Rclone Data Directory
rclone_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/Rclone"

# -- Time Machine Data Directory
time_machine_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/TimeMachine"

# -- Traefik Data Directory
traefik_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/Traefik"

# -- Vault Data Directory
vault_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/Vault"

# -- Vector Data Directory
vector_data_dir: "{{ zfs_zpool_base_dir }}/Apps/Internal/Vector"

# -- Vector Aggregator Address
vector_aggregator_addr: "192.168.69.111"

# -- Vector Aggregator Port
vector_aggregator_port: "6000"
