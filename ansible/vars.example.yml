---

ansible_user: "devin"

timezone: "America/New_York"

ntp_servers:
  primary:
    - time.cloudflare.com
    - time.google.com
  fallback:
    - 0.us.pool.ntp.org
    - 1.us.pool.ntp.org
    - 2.us.pool.ntp.org
    - 3.us.pool.ntp.org    

remote_syslog:
  enabled: true
  ip: 192.168.42.155
  port: 1514