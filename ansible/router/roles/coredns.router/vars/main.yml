---
#
# SECRET* vars are encrypted with sops
#

# TODO: Renovate coredns version in router ansible role
coredns_version: 0.3.0
coredns_download_url: https://github.com/onedr0p/opnsense-coredns/releases/download/v{{ coredns_version }}/opnsense-coredns_{{ coredns_version }}_freebsd_amd64.tar.gz
