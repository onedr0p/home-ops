# yaml-language-server: $schema=https://json.schemastore.org/ansible-playbook.json
---
- hosts: 127.0.0.1
  connection: local
  gather_facts: no
  become: no
  any_errors_fatal: true
  roles:
    - role: coredns.local
