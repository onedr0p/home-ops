---
data_dir: /tmp
sources:
  journal_logs:
    type: journald
    journal_directory: /run/log/journal
sinks:
  vector_sink:
    type: vector
    inputs:
      - journal_logs
    address: "{{ vector_aggregator_addr }}:{{ vector_aggregator_port }}"
    version: "2"
