---

# SECRET_MINIO_ADDR: [string]
# SECRET_MINIO_ACCESS_KEY: [string]
# SECRET_MINIO_SECRET_KEY: [string]

# k3s_etcd_s3: [bool]
# k3s_etcd_s3_bucket: [string]
# k3s_etcd_s3_insecure: [bool]
# k3s_etcd_snapshot_schedule_cron: [string]
# k3s_etcd_snapshot_retention: [int]

# -- Encapsulation type [string]
# calico_encapsulation:
# -- BGP Peer IP [string]
# -- (usually your router IP address)
# calico_bgp_peer_ip:
# -- BGP Autonomous System Number [int]
# -- (must be the same across all BGP peers)
# calico_bgp_as_number:
# -- BGP Network you want services to consume [string]
# -- (this network should not exist or be defined anywhere in your network)
# calico_bgp_external_ips:
# -- CIDR of the host node interface Calico should use [string]
# calico_node_cidr:

# os_timezone: [string]
# os_ssh_authorized_keys: [list]
# os_packages_install: [list]
# os_packages_remove: [list]
