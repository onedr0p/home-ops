---

- hosts:
  - control-nodes
  - generic-nodes
  become: true
  gather_facts: true
  any_errors_fatal: true
  roles:
  - ubuntu
  tasks:
  - name: reboot
    ansible.builtin.reboot:
    ignore_errors: true
