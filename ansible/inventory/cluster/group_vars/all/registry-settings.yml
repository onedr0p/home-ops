---

# Configure a pull thru cache using an existing registry
# ...requires a registry cache already set up
registry:
  cache:
    enabled: true
    address: "http://192.168.1.39:8082"
  custom:
    enabled: false
    address: "http://192.168.1.39:5000"
    username: "admin"
    password: "password"
