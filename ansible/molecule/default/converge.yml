---

# - name: Converge
#   hosts: all

- name: Converge
  hosts: all
  become: true
  vars:
    molecule_is_test: true
    ansible_user: root
  #   k3s_server:
  #     https-listen-port: 26443
  #     cluster-domain: examplecluster.local
  #   k3s_agent:
  #     docker: true
  roles:
    - role: "../../roles/ubuntu"

# - import_playbook: ../../playbooks/ubuntu/prepare.yml
