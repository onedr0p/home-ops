version: '2'

tasks:
  update-secrets:
    cmds:
      - ./secrets/helm-secrets.sh
      - ./secrets/k8s-secrets.sh
      - ./secrets/sample-env.sh
    silent: true
  sync-flux:
    cmds:
      - fluxctl --k8s-fwd-ns=flux sync 
  